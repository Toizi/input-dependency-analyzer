Compile:

Create a build directory
cd build
cmake ..
make

Run:
[cc -c rtlib.c]  **if any runtime library** 
clang -Xclang -load -Xclang build/OH/libOHPass.so -c example.c [-emit-llvm]
cc example.o rtlib.o *if wanna run*


Print dot files:
opt --dot-cfg file.s
dot -Tpng cfg.file.dot>output.png
xdg-open cfg.file.dot

